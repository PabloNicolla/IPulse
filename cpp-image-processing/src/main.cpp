#include <iostream>
#include <thread>
#include <chrono>
#include "testing.h"

int main()
{
    std::cout << "C++ Image Processing Placeholder" << sum(0, 0) << std::endl;
    std::cout << "C++ Image Processing Placeholder" << sum(1, 0) << std::endl;
    std::cout << "C++ Image Processing Placeholder" << sum(1, 1) << std::endl;
    std::cout << "C++ Image Processing Placeholder" << sum(2, 1) << std::endl;
    // std::cout << "Starting sleep...\n";
    // std::this_thread::sleep_for(std::chrono::seconds(50));
    // std::cout << "Sleep finished!\n";
    return 0;
}
